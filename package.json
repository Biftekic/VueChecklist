{
  "name": "vue-checklist-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd vue-checklist-app && npm run dev",
    "build": "cd vue-checklist-app && npm install && npm run build",
    "preview": "cd vue-checklist-app && npm run preview",
    "install": "cd vue-checklist-app && npm install"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vue/tsconfig": "^0.8.0",
    "typescript": "^5.9.2",
    "vue-tsc": "^3.0.5"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
